const parentContainer = document.querySelector('.parent')

parentContainer.innerHTML=`
<div class = 'child'>
<div class = 'inner-child'></div>
</div>`

// const childContainer= document.createElement('div') //created the div element
// const innerChildContainer = document.createElement ('div')


// childContainer.classList.add ('child')
// childContainer.innerText= 'hello'

// innerChildContainer.classList.add('innerchild')

// parentContainer.appendChild(childContainer)
// childContainer.appendChild(innerChildContainer)

// console.log(childContainer)